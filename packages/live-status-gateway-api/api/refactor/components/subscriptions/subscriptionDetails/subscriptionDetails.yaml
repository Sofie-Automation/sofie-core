$defs:
  subscriptionDetails:
    type: object
    title: SubscriptionDetails
    properties:
      name:
        $ref: '../subscriptionName.yaml'
      status:
        $ref: '../subscriptionStatusName.yaml'
    required: [name, status]
    additionalProperties: false
    examples:
      - $ref: './subscriptionDetails-example.yaml'

description: Topic for active playlist updates

subscribe:
  operationId: receiveActivePlaylist
  summary: Server sends active playlist events
  message:
    oneOf:
      - $ref: '../../components/playlist/messages/activePlaylist/activePlaylist.yaml#/components/messages/activePlaylist'
